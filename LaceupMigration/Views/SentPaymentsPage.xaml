<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels"
             x:Class="LaceupMigration.Views.SentPaymentsPage"
             Shell.TabBarIsVisible="False"
             Title="Sent Payments">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" RowSpacing="8">
        <!-- Search Bar -->
        <SearchBar Grid.Row="0"
                   Text="{Binding SearchQuery}"
                   Placeholder="Search by client name or payment type..."
                   Margin="8,8,8,0" />

        <!-- Buttons Layout -->
        <HorizontalStackLayout Grid.Row="1"
                               Spacing="8"
                               Margin="8,0"
                               IsVisible="{Binding ShowButtonsLayout}">
            <Button Text="Print"
                    Command="{Binding PrintCommand}"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Email"
                    Command="{Binding SendByEmailCommand}"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Resend"
                    Command="{Binding ResendCommand}"
                    HorizontalOptions="FillAndExpand" />
        </HorizontalStackLayout>

        <!-- Select All Layout -->
        <Frame Grid.Row="2"
               Padding="12"
               Margin="8,0"
               HasShadow="False"
               BackgroundColor="{StaticResource Gray100}">
            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                <CheckBox Grid.Column="0"
                          IsChecked="{Binding IsSelectAllChecked}"
                          CheckedChanged="CheckBox_CheckedChanged"
                          VerticalOptions="Center" />
                <Grid Grid.Column="1" ColumnDefinitions="*,Auto">
                    <Label Grid.Column="0"
                           Text="{Binding SelectAllText}"
                           VerticalOptions="Center"
                           FontSize="14" />
                    <Label Grid.Column="1"
                           Text="{Binding TotalText}"
                           FontAttributes="Bold"
                           FontSize="14"
                           IsVisible="{Binding ShowTotal}"
                           VerticalOptions="Center" />
                </Grid>
            </Grid>
        </Frame>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="4"
                           IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           Color="{StaticResource Primary}"
                           VerticalOptions="Center"
                           HorizontalOptions="Center" />

        <!-- Sent Payments List -->
        <CollectionView Grid.Row="4"
                        ItemsSource="{Binding SentPayments}"
                        Margin="8,0,8,8">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:SentPaymentItemViewModel">
                    <Frame Padding="12"
                           Margin="0,4"
                           BackgroundColor="{StaticResource Gray100}"
                           HasShadow="False"
                           CornerRadius="8"
                           InputTransparent="False">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding ViewDetailsCommand}" />
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12" InputTransparent="False">
                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding IsChecked}"
                                      VerticalOptions="Center" />

                            <VerticalStackLayout Grid.Column="1" Spacing="4" InputTransparent="False">
                                <Label Text="{Binding ClientName}"
                                       FontSize="16"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding PaymentTypeText}"
                                       FontSize="14"
                                       TextColor="#666666" />
                                <Label Text="{Binding AmountText}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ShowTotal}" />
                                <Label Text="{Binding DateText}"
                                       FontSize="13"
                                       TextColor="#666666" />
                                <Label Text="{Binding CommentText}"
                                       FontSize="13"
                                       TextColor="#666666"
                                       IsVisible="{Binding ShowComment}" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>

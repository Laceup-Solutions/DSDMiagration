<?xml version="1.0" encoding="utf-8" ?>
<local:LaceupContentPage xmlns:local="clr-namespace:LaceupMigration.Views" xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels"
             x:Class="LaceupMigration.Views.AddPOSToRoutePage"
             Shell.TabBarIsVisible="False"
             Title="Add Positions to Route">
    <Grid RowDefinitions="Auto,Auto,*" RowSpacing="8">
        <SearchBar Grid.Row="0"
                   Text="{Binding SearchText}"
                   Placeholder="Search clients..."
                   BackgroundColor="White"
                   Margin="8"
                   TextChanged="OnSearchTextChanged" />

        <HorizontalStackLayout Grid.Row="1"
                               Spacing="8"
                               Margin="8,0">
            <Button Text="Select All"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SelectAllCommand}"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Add Selected"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding AddSelectedCommand}"
                    HorizontalOptions="FillAndExpand" />
        </HorizontalStackLayout>

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding Clients}"
                        Margin="8">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:ClientRouteItemViewModel">
                    <Frame Padding="12"
                           Margin="0,4"
                           BackgroundColor="{StaticResource Gray100}"
                           HasShadow="False"
                           CornerRadius="8">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="8">
                            <CheckBox Grid.Column="0"
                                     IsChecked="{Binding IsSelected}"
                                     VerticalOptions="Center" />
                            <VerticalStackLayout Grid.Column="1" Spacing="4">
                                <Label Text="{Binding ClientName}"
                                       FontSize="16"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding Address}"
                                       FontSize="14"
                                       TextColor="#666666" />
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</local:LaceupContentPage>


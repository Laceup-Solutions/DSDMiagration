<?xml version="1.0" encoding="utf-8" ?>
<local:LaceupContentPage xmlns:local="clr-namespace:LaceupMigration.Views" xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels"
             x:Class="LaceupMigration.Views.SentOrdersPage"
             Shell.TabBarIsVisible="False"
             x:DataType="vm:SentOrdersPageViewModel"
             Title="Sent Orders">
    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Button">
                <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="8" />
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,Auto,Auto,*" RowSpacing="8">
        <!-- Search Bar -->
        <SearchBar Grid.Row="0"
                   Text="{Binding SearchQuery}"
                   Placeholder="Search by client name or invoice number..."
                   BackgroundColor="White"
                   Margin="8,8,8,0" />

        <!-- Buttons Layout -->
        <HorizontalStackLayout Grid.Row="1"
                               Spacing="5"
                               Margin="8,0"
                               IsVisible="{Binding ShowButtonsLayout}">
            
            <Button
                    Style="{StaticResource PrimaryButton}"
                    WidthRequest="100"
                    Text="{Binding SelectedTransactionType}"
                    Command="{Binding SelectTransactionTypeCommand}"
                    HorizontalOptions="Start"
                    VerticalOptions="Center"
                    Padding="12,8" />
            
            <Button Text="Print"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding PrintCommand}"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Email"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SendByEmailCommand}"
                    HorizontalOptions="FillAndExpand" />
            <Button Text="Resend"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding ResendCommand}"
                    HorizontalOptions="FillAndExpand" />
        </HorizontalStackLayout>

        <!-- Select All Layout -->
   
            <Grid Grid.Row="2" ColumnDefinitions="Auto,*, auto" ColumnSpacing="5">
                <CheckBox Grid.Column="0"
                          IsChecked="{Binding IsSelectAllChecked}"
                          CheckedChanged="CheckBox_CheckedChanged"
                          VerticalOptions="Center" />
                <Label Grid.Column="1"
                       Text="{Binding SelectAllText}"
                       HorizontalTextAlignment="Start"
                       HorizontalOptions="Start"
                       VerticalOptions="Center"
                       FontSize="16" />
                
                <HorizontalStackLayout Grid.Column="2" Spacing="4" VerticalOptions="Center" Margin="0,0,10,0">
                    <Label Text="Date:" FontSize="12" TextColor="Gray" VerticalOptions="Center" />
                    <DatePicker Date="{Binding SelectedDate}"
                                Format="MM/dd/yyyy"
                                MinimumDate="{Binding MinDate}"
                                MaximumDate="{Binding MaxDate}" />
                </HorizontalStackLayout>
            </Grid>

        <!-- Loading Indicator -->
        <ActivityIndicator Grid.Row="3"
                           IsRunning="{Binding IsLoading}"
                           IsVisible="{Binding IsLoading}"
                           Color="{StaticResource Primary}"
                           VerticalOptions="Center"
                           HorizontalOptions="Center" />

        <!-- Sent Orders List -->
        <CollectionView Grid.Row="3"
                        ItemsSource="{Binding SentOrders}"
                        Margin="8,0,8,8">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:SentOrderItemViewModel">
                    <Border Padding="12"
                           Margin="0,4"
                           StrokeShape="RoundRectangle 8">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <CheckBox Grid.Column="0"
                                      IsChecked="{Binding IsChecked}"
                                      VerticalOptions="Center" />

                            <VerticalStackLayout Grid.Column="1" 
                                                  Spacing="4"
                                                  VerticalOptions="Center">
                                <VerticalStackLayout.GestureRecognizers>
                                    <TapGestureRecognizer Command="{Binding ViewDetailsCommand}" />
                                </VerticalStackLayout.GestureRecognizers>
                                <Label Text="{Binding DateClientText}"
                                       FontSize="16"
                                       FontAttributes="Bold" />
                                <Label Text="{Binding TransactionType}"
                                       FontSize="14"
                                       TextColor="#666666" />
                                <Label Text="{Binding OrderNumberText}"
                                       FontSize="14"
                                       TextColor="#666666"
                                       IsVisible="{Binding ShowOrderNumber}" />
                                <Label Text="{Binding Total, StringFormat='Total: {0}'}"
                                       FontSize="14"
                                       FontAttributes="Bold"
                                       IsVisible="{Binding ShowTotal}" />
                            </VerticalStackLayout>
                            
                            <Button Grid.Column="2"
                                    Text="→"
                                    FontSize="18"
                                    BackgroundColor="Transparent"
                                    TextColor="#1976D2"
                                    Command="{Binding ViewDetailsCommand}"
                                    WidthRequest="40"
                                    HeightRequest="40"
                                    Padding="0" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</local:LaceupContentPage>

<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="LaceupMigration.Views.SelfService.SelfServiceCatalogPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels.SelfService"
             x:DataType="vm:SelfServiceCatalogPageViewModel"
             Title="Products">
    <Grid RowDefinitions="Auto,Auto,Auto,Auto,*" Padding="10">
        <Label Grid.Row="0" 
               Text="{Binding ClientName}" 
               FontSize="18" 
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>
        
        <Label Grid.Row="1" 
               Text="{Binding CategoryName}" 
               FontSize="16" 
               TextColor="Gray"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>
        
        <Grid Grid.Row="2" ColumnDefinitions="*,Auto" ColumnSpacing="8" Margin="0,0,0,10">
            <SearchBar Grid.Column="0" 
                       Placeholder="Search products..."
                       Text="{Binding SearchText}"
                       TextChanged="OnSearchTextChanged"/>
            <Button Grid.Column="1" 
                    Text="Scan"
                    Command="{Binding ScanCommand}"
                    WidthRequest="80"/>
        </Grid>
        
        <HorizontalStackLayout Grid.Row="3" Spacing="8" Margin="0,0,0,10">
            <Button Text="{Binding FilterText}" 
                    Command="{Binding FilterCommand}"
                    HorizontalOptions="FillAndExpand"/>
            <Button Text="{Binding SortByText}" 
                    Command="{Binding SortByCommand}"
                    HorizontalOptions="FillAndExpand"/>
        </HorizontalStackLayout>
        
        <CollectionView Grid.Row="4" 
                        ItemsSource="{Binding Products}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:ProductItemViewModel">
                    <Frame Padding="12" 
                           Margin="0,4" 
                           BackgroundColor="{StaticResource Gray100}"
                           HasShadow="False"
                           CornerRadius="8">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelfServiceCatalogPageViewModel}}, Path=SelectProductCommand}" 
                                                  CommandParameter="{Binding}"/>
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding ProductName}" 
                                       FontSize="16" 
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding CategoryName}" 
                                       FontSize="14" 
                                       TextColor="Gray"/>
                                <Label Text="{Binding PriceText}" 
                                       FontSize="14" 
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding StatusText}" 
                                       FontSize="12" 
                                       TextColor="{Binding StatusColor}"/>
                            </VerticalStackLayout>
                            <Label Grid.Column="1" 
                                   Text="{Binding QtyInOrder}" 
                                   FontSize="18" 
                                   FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>


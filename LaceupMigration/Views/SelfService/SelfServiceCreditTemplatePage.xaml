<?xml version="1.0" encoding="utf-8" ?>
<local:LaceupContentPage xmlns:local="clr-namespace:LaceupMigration.Views" x:Class="LaceupMigration.Views.SelfService.SelfServiceCreditTemplatePage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels.SelfService"
             x:DataType="vm:SelfServiceCreditTemplatePageViewModel"
             Title="Credit Items">
    <Grid RowDefinitions="Auto,Auto,Auto,Auto,Auto,Auto,Auto,Auto,*" Padding="10">
        <Label Grid.Row="0" 
               Text="{Binding ClientName}" 
               FontSize="18" 
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1" ColumnDefinitions="*,*,*" ColumnSpacing="8" Margin="0,0,0,10">
            <Label Grid.Column="0" 
                   Text="{Binding LinesText}" 
                   FontSize="14"/>
            <Label Grid.Column="1" 
                   Text="{Binding QtyText}" 
                   FontSize="14"/>
            <Label Grid.Column="2" 
                   Text="{Binding TermText}" 
                   FontSize="14"/>
        </Grid>
        
        <Grid Grid.Row="2" ColumnDefinitions="*,*" ColumnSpacing="8" Margin="0,0,0,10" IsVisible="{Binding ShowPrices}">
            <Label Grid.Column="0" 
                   Text="{Binding SubtotalText}" 
                   FontSize="14"/>
            <Label Grid.Column="1" 
                   Text="{Binding DiscountText}" 
                   FontSize="14"/>
        </Grid>
        
        <Grid Grid.Row="3" ColumnDefinitions="*,*" ColumnSpacing="8" Margin="0,0,0,10" IsVisible="{Binding ShowPrices}">
            <Label Grid.Column="0" 
                   Text="{Binding TaxText}" 
                   FontSize="14"/>
            <Label Grid.Column="1" 
                   Text="{Binding TotalText}" 
                   FontSize="16" 
                   FontAttributes="Bold"/>
        </Grid>
        
        <HorizontalStackLayout Grid.Row="4" Spacing="8" Margin="0,0,0,10">
            <Button Text="History" 
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding HistoryCommand}"
                    HorizontalOptions="FillAndExpand"/>
            <Button Text="Catalog" 
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding CatalogCommand}"
                    HorizontalOptions="FillAndExpand"/>
            <Button Text="Search" 
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding SearchCommand}"
                    HorizontalOptions="FillAndExpand"/>
        </HorizontalStackLayout>
        
        <Button Grid.Row="5" 
                Style="{StaticResource PrimaryButton}"
                Text="Send" 
                Command="{Binding SendCommand}"
                Margin="0,0,0,10"/>
        
        <CollectionView Grid.Row="6" 
                        ItemsSource="{Binding OrderLines}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:OrderLineItemViewModel">
                    <Frame Padding="12" 
                           Margin="0,4" 
                           BackgroundColor="{StaticResource Gray100}"
                           HasShadow="False"
                           CornerRadius="8">
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <VerticalStackLayout Grid.Column="0" Spacing="4">
                                <Label Text="{Binding ProductName}" 
                                       FontSize="16" 
                                       FontAttributes="Bold"/>
                                <Label Text="{Binding QtyText}" 
                                       FontSize="14" 
                                       TextColor="Gray"/>
                                <Label Text="{Binding PriceText}" 
                                       FontSize="14"
                                       IsVisible="{Binding Source={RelativeSource AncestorType={x:Type vm:SelfServiceCreditTemplatePageViewModel}}, Path=ShowPrices}"/>
                            </VerticalStackLayout>
                            <Button Grid.Column="1" 
                                    Text="Remove" 
                                    Style="{StaticResource DeleteButton}"
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelfServiceCreditTemplatePageViewModel}}, Path=RemoveLineCommand}"
                                    CommandParameter="{Binding}"
                                    WidthRequest="80"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</local:LaceupContentPage>


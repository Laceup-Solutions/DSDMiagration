<?xml version="1.0" encoding="utf-8" ?>
<local:LaceupContentPage xmlns:local="clr-namespace:LaceupMigration.Views" x:Class="LaceupMigration.Views.SelfService.SelfServiceCategoriesPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:LaceupMigration.ViewModels.SelfService"
             x:DataType="vm:SelfServiceCategoriesPageViewModel"
             Title="Categories">
    <Grid RowDefinitions="Auto,Auto,*" Padding="10">
        <Label Grid.Row="0" 
               Text="{Binding ClientName}" 
               FontSize="18" 
               FontAttributes="Bold"
               HorizontalOptions="Center"
               Margin="0,0,0,10"/>
        
        <Grid Grid.Row="1" ColumnDefinitions="*,Auto" ColumnSpacing="8" Margin="0,0,0,10">
            <SearchBar Grid.Column="0" 
                       Placeholder="Search categories..."
                       Text="{Binding SearchText}"
                       BackgroundColor="White"
                       TextChanged="OnSearchTextChanged"/>
            <Button Grid.Column="1" 
                    Text="Filter"
                    Command="{Binding FilterCommand}"
                    WidthRequest="80"/>
        </Grid>
        
        <CollectionView Grid.Row="2" 
                        ItemsSource="{Binding Categories}">
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="vm:CategoryItemViewModel">
                    <Frame Padding="12" 
                           Margin="0,4" 
                           BackgroundColor="{StaticResource Gray100}"
                           HasShadow="False"
                           CornerRadius="8">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:SelfServiceCategoriesPageViewModel}}, Path=SelectCategoryCommand}" 
                                                  CommandParameter="{Binding}"/>
                        </Frame.GestureRecognizers>
                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="12">
                            <Label Grid.Column="0" 
                                   Text="{Binding CategoryName}" 
                                   FontSize="16" 
                                   FontAttributes="Bold"/>
                            <Label Grid.Column="1" 
                                   Text="→" 
                                   FontSize="18" 
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</local:LaceupContentPage>

